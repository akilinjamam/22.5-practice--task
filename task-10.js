// showing numbers larger  than 80 using for loop

numbers = [10, 30, 50, 70, 90, 100, 110, 120, 130];

for (let i = 0; i < 9; i++) {
    let number = numbers[i]
    if (number > 80) {
        console.log(number);

    }
}